# ♻️ 部品マスタの使用製品を used_in_ai/mini フラグから ProductBOM 導出へ正規化

| 項目 | 内容 |
|------|------|
| **日時** | 2026-07-07 21:06 |
| **カテゴリ** | `refactor` |
| **エンジニア** | Hisato Matsuo |
| **プロジェクト** | `bomops` |

## 📝 作業サマリー

部品マスタを製品の family/grade/variation 4層モデルに適応させた。BAITEN 固有のハードコードフラグ `used_in_ai`/`used_in_mini` は RISC-V 等の新ファミリに拡張できないため、CLAUDE.md 正規化原則（タグ複製ではなく関係で表現）に従い「どの製品で使うか」を **ProductBOM 関係からの導出** に置き換えた。

**バックエンド:**
- マイグレーション0005: `used_in_ai`/`used_in_mini` フィールドを削除（情報は本番 ProductBOM 18行に反映済みのため実質損失なし。Notion 由来のフラグは import 時に `spec_json.notion_used_in` へ記録保持）
- `PartMasterSerializer` に **`used_in`** を追加: ProductBOM 由来の使用先リスト（product_model/code/name/family/grade/variation/quantity/is_optional）。N+1回避に `prefetch_related("bom_usages__product_model__family")`
- フィルタ3種追加: `used_in_model`（モデルID）/ `used_in_family`（ファミリ名）/ `used_in_grade`（グレード）— いずれも distinct
- `derive_bom` からフラグ突合ロジック（flagged_parts_for・必須昇格・未観測追加）を削除し観測ベースのみに簡素化（ProductBOM 正本化で役目終了）
- `import_notion` からフラグ設定を除去、admin の使用モデル fieldset 削除
- テスト修正（derive_bom のフラグ期待を観測のみに書き換え）— **47/47 パス**

**フロントエンド:**
- 一覧「使用モデル」列 →「使用製品」列: BOM 導出バッジ（グレード名表示・必須=sky/任意=pale）。例: DIS-M001 → `AI` `Mini`
- フィルタバーに「使用製品」セレクト追加（`used_in_model`）— 「BSTAND-AI で使う部品」のピンポイント絞り込み
- 編集ドロワーの AI/Mini チェックボックス撤去（使用関係の編集は製品モデル詳細の BOM 編集が正本）

**検証:** フロント23/23・lint/build クリーン。ライブAPIで used_in 導出（DIS-M001→AI必須/Mini必須）、used_in_model=BSTAND-AI→10部品、used_in_family=BAITEN STAND→14部品 を確認。CLAUDE.md（対応表・derive_bom コマンド説明）更新。

**留意:** 未コミット・本番未デプロイ。本番反映時はマイグレーション0005でフラグ列が削除される（不可逆だが Notion extracts から再取得可能・ProductBOM に反映済み）。

## 💬 使用したプロンプト

**[1]** now we have to modify 部品マスタ to adapt to the family/grade/variation models. can you go?

## 📁 変更ファイル

- `bomops_core/bom/models.py`（used_in_ai/mini 削除）
- `bomops_core/bom/migrations/0005_remove_partmaster_used_in_ai_and_more.py`（新規）
- `bomops_core/bom/serializers.py`（PartMasterUsedInSerializer・used_in）
- `bomops_core/bom/views.py`（used_in_* フィルタ・prefetch）
- `bomops_core/bom/management/commands/derive_bom.py`（フラグ突合削除）
- `bomops_core/bom/management/commands/import_notion.py`（spec_json 記録化）
- `bomops_core/bom/{admin,tests}.py`
- `frontend/web/src/features/workspace/parts/{PartMasterListPage,PartMasterFormDrawer}.tsx`
- `frontend/web/src/api/schema.d.ts` / `docs/openapi.yaml`（再生成）/ `CLAUDE.md`

---
_Generated by /save-task · 2026-07-07T21:06:41+09:00_
