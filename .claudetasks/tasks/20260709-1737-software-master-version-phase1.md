# 🚀 ソフトウェアを一級部品として追加（ソフトウェアマスタ＋バージョン・Phase 1）

| 項目 | 内容 |
|------|------|
| **日時** | 2026-07-09 17:37 |
| **カテゴリ** | `tasks` |
| **エンジニア** | Hisato Matsuo |
| **プロジェクト** | `bomops` |

## 📝 作業サマリー

BOMOps に「ソフトウェア部品」を導入する新規アイデアを議論し、Phase 1（マスタ＋バージョンのカタログ）を実装した。粒度は粗く（アプリスタック単位、個々のアプリは端末側 config で切替）。

**設計議論の結論:**
- ソフトはコピー可能なため、ハードの「実物（部品実物）」と違い「バージョン（リリース）」と「搭載（どの台に何版）」が別概念 → ソフトはハードより1階層多い（Master → Version → Install）
- 段階: Phase 1=マスタ＋バージョン（今回）／Phase 2=製品セットへの SoftwareInstall＋更新履歴／Phase 3=製品BOMにソフトを含める（合意済み・未実装）

**データモデル（migration 0009）:**
- `SoftwareMaster`（部品マスタのソフト版）: code / name / kind（STACK/FIRMWARE/OS/OTHER）/ vendor / description / is_active
- `SoftwareVersion`（部品実物のソフト版・ただしリリース単位で多数台共有）: software FK / version / status（RELEASED/BETA/DEPRECATED）/ release_date / artifact_ref / notes。unique_together=(software, version)

**API:** `/software-masters/`（version_count annotate・kind/検索フィルタ）、`/software-versions/`（software/status/検索フィルタ）。admin 登録。

**UI（ハードと対の骨格）:**
- ソフトウェアマスタ（マスタ群メニュー）: 一覧＋作成/編集ドロワー、行の「バージョン一覧」ボタン
- ソフトウェアバージョン（運用群メニュー・部品実物と同層）: ソフト選択→バージョン一覧のドリルダウン（?software=<id> でフレーム切替・戻る・追加/編集）
- サイドメニュー マスタ/運用 に各1項目追加、ルート2本追加

**発生した不具合と対処:** 既存 ProductModelViewSet 用の @extend_schema_view デコレータが挿入位置ずれで SoftwareMasterFilter に付き全URL解決が失敗 → デコレータを ProductModelViewSet 直上へ戻して解消（テスト全緑）。

**検証:** バックエンド **68/68**（マスタ version_count・バージョンフィルタ・重複制約の3テスト新規）、フロント23/23、lint/build クリーン。ローカルに BSTAND-SW（v3件）/RISCV-FW（v1件）を投入し API/ドリルダウンを確認。

**残課題:** 未コミット・本番未デプロイ（本番反映時 migrate 0009）。Phase 2/3 は後続。

## 💬 使用したプロンプト

**[1]** let us think about "software" parts. ... In the same layer of 部品マスタ, separately add ソフトウェアマスタ. And in the same layer of 部品実物, software versions are managed. ... let us discuss.
**[2]** 1. from Phase 1 first, 2. yes, 3. アプリ単位はconfigで切り替えるので荒い方で行きましょう

## 📁 変更ファイル

- `bomops_core/bom/models.py`（SoftwareMaster / SoftwareVersion）
- `bomops_core/bom/migrations/0009_softwaremaster_softwareversion.py`（新規）
- `bomops_core/bom/{serializers,views,urls,admin,tests}.py`
- `frontend/web/src/features/workspace/software/`（新規: 一覧×2・フォーム×2）
- `frontend/web/src/components/AppLayout.tsx`（メニュー）/ `src/App.tsx`（ルート）
- `frontend/web/src/api/{types.ts,schema.d.ts}` / `docs/openapi.yaml`（再生成）

---
_Generated by /save-task · 2026-07-09T17:37:22+09:00_
