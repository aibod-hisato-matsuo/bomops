# 🚀 「製品セット」への呼称変更と納品先エリア（国→顧客→拠点）カスケード

| 項目 | 内容 |
|------|------|
| **日時** | 2026-07-07 22:06 |
| **カテゴリ** | `tasks` |
| **エンジニア** | Hisato Matsuo |
| **プロジェクト** | `bomops` |

## 📝 作業サマリー

多製品化に伴い BAITEN 固有名だった「BSSセット」を「製品セット」に改称し、グローバル展開を見据えた納品先エリア情報（国→顧客→拠点）を導入した。

**設計判断:**
- 呼称変更は**表示名のみ**（メニュー・ホームマップ・全画面・admin・APIタグ）。コード識別子（`BssSet`・`bss_set`・`/bss-sets/`）はDB/API破壊を避けて不変とし、CLAUDE.md 対応表に明記（コード改名は将来の専用リファクタリングへ）
- エリアの3層のうち「顧客」「拠点」は既存の `Customer`/`CustomerSite` がそのまま該当（タグ体系を新設しない＝正規化原則）。**欠けていた「国」だけを `CustomerSite.country` として追加**（ISO 3166-1 alpha-2・既定JP）。国は物理的実体である拠点に置き、多国籍顧客（日本企業の米国工場等）にも対応

**バックエンド:**
- マイグレーション0006: country 追加（既存20拠点は既定値JP）＋verbose_name変更
- 新API `/bss-sets/location-summary/`（国×顧客×拠点の件数集計。在庫中セットは null 行）
- フィルタ追加: セット `country`（iexact）/`customer`、拠点 `country`。セットAPIに `site_country`
- テスト3件新規（country既定値・location-summary・country/customerフィルタ）— **50/50 パス**

**フロントエンド:**
- 製品セット一覧に**納品先カスケード**（CascadeRow 再利用）: 国→顧客→拠点、件数付き・URL同期。実データで JP(40)→AIBOD(27)→AIBOD大名(15) 等を確認。在庫6台は「全て」でのみ表示
- 一覧に「国」列追加、拠点編集フォームに国コード入力（2文字regex・保存時大文字化・既定JP）
- 全画面の「BSSセット」表記を「製品セット」に一括変更

**ドキュメント:** CLAUDE.md 対応表（DeviceSet 行に表示名注記・Site 行に country）と正準ER図（Site.country）を更新。

**検証:** バックエンド50/50・フロント23/23・lint/build クリーン。ライブAPIで国×顧客×拠点集計・JPフィルタ40台を確認。

**留意:** 未コミット・本番未デプロイ（確認待ち。本番反映時はマイグレーション0006で既存拠点が自動的にJPになる）。

## 💬 使用したプロンプト

**[1]** now move on to BSSセット. We have taken several products into 製品モデル. So "BSSセット" name should be "製品セット" or other apprepriate naming. Also I want to introduce area/location information tags where the product is used/delivered. Considering our future business, area is better to be global. Therefore, area will be "country" (like JP or US), "company or customer" (like AIBOD or 東北技研 or 松本工業, entity that purchases the product), "location" (like 大熊ホテルVerde, 豊前工場 that the product is used). what do you think?（→ 表示名のみ変更＋country追加＋既存関係の再利用を提案）
**[2]** 了解です。お願いします

## 📁 変更ファイル

- `bomops_core/bom/models.py`（CustomerSite.country・verbose_name 製品セット化）
- `bomops_core/bom/migrations/0006_..._and_more.py`（新規）
- `bomops_core/bom/{serializers,views,admin,tests}.py`（location-summary・フィルタ・表示名）
- `frontend/web/src/features/workspace/sets/*`（製品セット表記・納品先カスケード・国列）
- `frontend/web/src/features/workspace/customers/SiteFormDrawer.tsx`（国コード入力）
- `frontend/web/src/components/AppLayout.tsx` / `WorkspaceHome.tsx` / `SearchPage.tsx`（表記）
- `frontend/web/src/api/{types.ts,schema.d.ts}` / `docs/openapi.yaml`（再生成）/ `CLAUDE.md`

---
_Generated by /save-task · 2026-07-07T22:06:40+09:00_
