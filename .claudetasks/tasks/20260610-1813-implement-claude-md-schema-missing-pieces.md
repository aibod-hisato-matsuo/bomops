# 🚀 CLAUDE.mdスキーマ準拠の未実装モデル群（SiteConfig/運用履歴等）を実装

| 項目 | 内容 |
|------|------|
| **日時** | 2026-06-10 18:13 |
| **カテゴリ** | `tasks` |
| **エンジニア** | Hisato Matsuo |
| **プロジェクト** | `bomops` |

## 📝 作業サマリー

前セッションのギャップ分析に基づき、CLAUDE.md 正準スキーマの未実装部分を実装した（正準名は既存実装名にマッピング: Site→CustomerSite, DeviceSet→BssSet, Unit→PartUnit）。

**新規モデル（4つ）:**
- `SiteConfig` — CustomerSite と 1:1。Loyverse/Square/PayPay/BAITEN Cloud/Google/Slack のクレデンシャルと config_toml/baiten_env を保持。secret系6フィールドは新設の `EncryptedTextField`（bom/fields.py、Fernet暗号化）で暗号化保存し、APIレスポンスでは `****`+末尾4文字にマスク（CLAUDE.md §4.1準拠）
- `MaintenanceEvent` — 追記型保守履歴（故障/交換/点検/設定変更）。FK は on_delete=PROTECT で履歴保全
- `DeployEvent` — 追記型導入履歴（設置/開通/稼働/回収/再組立）
- `EquipmentRef` — L4機器管理DB参照。PartUnit と N:M

**フィールド追加:**
- `CustomerSite.lifecycle_status`（準備中/稼働中/撤退済/拠点/貸出中）
- `PartMaster.used_in_ai` / `used_in_mini` / `size`

**追記型の強制（CLAUDE.md §4.3）:**
- イベント系 ViewSet は create/list/retrieve のみ（PUT/PATCH/DELETE は 405）
- Admin も change/delete 権限を無効化

**その他:**
- API 4ルート追加（site-configs / maintenance-events / deploy-events / equipment-refs）、フィルタ・OpenAPIアノテーション完備
- 初期マイグレーション `0001_initial.py` 生成（全13モデル）
- テスト13件追加（暗号化at-rest検証・APIマスク・1:1制約・ライフサイクル遷移・追記型405・N:M）→ 全24件パス
- `config/settings_sqlite.py` 追加（PostgreSQL なしでテスト実行可能: `pytest --ds=config.settings_sqlite`）
- CLAUDE.md に正準スキーマ⇔実装の対応表を追加、認証TODOをJWTに確定。README にエンドポイント追記

**スコープ判断:** 既存モデルの正準名へのリネーム（Customer→Organization 等）は「missing pieces」を超えるため見送り、対応表で管理。UnitSlotAssignment は既存 BssSetComponent.role で充足と判断。

## 💬 使用したプロンプト

**[1]** implement the missing pieces following CLAUDE.md schema

## 📁 変更ファイル

- `bomops/bom/models.py`（SiteConfig/MaintenanceEvent/DeployEvent/EquipmentRef 追加、PartMaster/CustomerSite フィールド追加）
- `bomops/bom/fields.py`（新規: EncryptedTextField）
- `bomops/bom/serializers.py`
- `bomops/bom/views.py`（AppendOnlyViewSet 基底クラス含む）
- `bomops/bom/urls.py`
- `bomops/bom/admin.py`
- `bomops/bom/tests.py`（+13テスト）
- `bomops/bom/migrations/0001_initial.py`（新規）
- `bomops/config/settings.py`（BOMOPS_ENCRYPTION_KEY）
- `bomops/config/settings_sqlite.py`（新規）
- `requirements.txt`（cryptography 追加）
- `.env.example` / `CLAUDE.md` / `README.md`

---
_Generated by /save-task · 2026-06-10T18:13:00+09:00_
