# 🚀 フロントエンドP6完了: 接続状態表示・エラー統一・ユニットテスト

| 項目 | 内容 |
|------|------|
| **日時** | 2026-06-11 11:54 |
| **カテゴリ** | `tasks` |
| **エンジニア** | Hisato Matsuo |
| **プロジェクト** | `bomops` |

## 📝 作業サマリー

フロントエンド実装計画（DOC-FE-001）の最終フェーズ P6 を完了。全6フェーズの DoD を達成した。

**ネットワーク耐性（network-resilience スキル準拠）:**
- 3層検知を実装: navigator.onLine イベント + axios クライアントの成功/失敗レポート（連続2回の無応答で offline 判定）+ ヘルスチェック（新設 `GET/HEAD /api/v1/health/`（認証不要・DBアクセスなし）を復帰時と5分毎にポーリング）
- 非ブロッキングのオフラインバナー（何が起きたか・表示データの状態・自動復旧を説明、閲覧は継続可能）+ 復帰時3秒間の通知
- TanStack Query のキャッシュ保持により取得失敗時も直前データを表示継続
- Outbox（書き込みキューイング）は社内管理ツールの性質上スコープ外と判断し README に明記

**エラーハンドリング統一:**
- QueryCache.onError で読み取りエラーをトースト通知（5秒スロットル・オフライン中はバナーが説明するため抑制）
- DataTable の「真っ白」アンチパターンを修正（取得失敗かつキャッシュなしで空白だった → 接続確認メッセージを表示）

**ユニットテスト（Vitest + Testing Library、23件）:**
- form-utils（payload整形・datetime往復変換・サーバエラーマッピング）
- parseApiErrors（フィールドエラー/409 detail/通信エラー/非axios例外）
- networkStore（失敗閾値・復帰・リスナー通知）
- DataTable（4つの描画状態 + 行クリック）
- LoginPage（バリデーション・ログイン成功遷移とトークン保存・401エラー表示）
- 発見した実問題: testing-library の自動クリーンアップには Vitest globals:true が必要（修正済）

**検証結果:** `npm run build` 警告ゼロ、lint クリーン（react-hooks の新ルール set-state-in-effect 違反を1件修正）、フロント 23/23・バックエンド 30/30 テストパス、health エンドポイントを Vite proxy 経由で実機確認。README（テストスクリプト・耐性設計・API一覧）と DOC-FE-001 changelog（v0.2）を更新。

## 💬 使用したプロンプト

**[1]** OK, proceed with P6

## 📁 変更ファイル

**フロントエンド:**
- `src/lib/network-store.ts`（新規: 3層検知ストア）
- `src/hooks/useNetworkStatus.ts`（新規）
- `src/components/OfflineBanner.tsx` / `.module.css`（新規）
- `src/api/client.ts`（成功/失敗レポート連携）
- `src/App.tsx`（QueryProvider 化・エラートースト統合）
- `src/components/AppLayout.tsx` / `.module.css`（バナー配置）
- `src/components/DataTable.tsx`（エラー状態表示）
- `src/main.tsx`（networkStore.start）
- `vite.config.ts`（Vitest設定）/ `src/test/setup.ts`（新規）
- テスト5ファイル（form-utils / errors / network-store / DataTable / LoginPage）
- `package.json`（test スクリプト・テスト依存）/ `README.md`

**バックエンド:**
- `bomops/bom/views.py`（health API）/ `bomops/bom/urls.py` / `bomops/bom/tests.py`（+1）
- `docs/openapi.yaml` / `src/api/schema.d.ts`（再生成）
- `docs/DOC-FE-001_bomops_frontend_plan.md`（changelog v0.2）
- `README.md`（カスタムAPI一覧・フロントエンド節）

---
_Generated by /save-task · 2026-06-11T11:54:00+09:00_
