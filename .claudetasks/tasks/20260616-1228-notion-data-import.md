# 🚀 Notion実データのインポートコマンド作成と本番DB取り込み

| 項目 | 内容 |
|------|------|
| **日時** | 2026-06-16 12:28 |
| **カテゴリ** | `tasks` |
| **エンジニア** | Hisato Matsuo |
| **プロジェクト** | `bomops` |

## 📝 作業サマリー

`from_notion/extracts/` の Notion エクスポート（5DB）をスキャン・評価し、冪等な取り込みコマンドを実装して本番 Cloud SQL へインポートした。

**スキャン・マッピング評価:**
- 5つのNotion DB（BOM 31 / Parts 151 / Product Set 47 / サービス管理 20 / SQUARE 1）を確認、ER図と一致
- Notionリレーションは `名前 (https://notion.so/...)` 形式 → URL除去で名前マッチ可能
- サービス管理の稼働値が lifecycle_status と完全一致、クレデンシャル列が SiteConfig と1:1

**承認された4つのマッピング判断:**
1. PartUnit識別子は Part名（実S/Nは note へ、重複Part名はサフィックス付与）
2. Notion BOM Category と仕様/商品説明は PartMaster.spec_json に格納（スキーマ変更なし）
3. Model列の混在（モデル＋状態）→ ProductModel 3種（AI/Mini/UNKNOWN）、状態は BssSet.status へ
4. 出荷日 → BssSet.installed_at

**実装: `manage.py import_notion` コマンド**
- `--dir` / `--dry-run` 対応、自然キーで update_or_create（冪等）、依存順に取り込み、created/updated/skipped レポート＋警告出力
- マッピング辞書（Type→Category、稼働→lifecycle、Model→status、SiteConfig列対応）

**発見・修正したバグ:** macOS のファイル名は NFD 正規化のため「サービス」を含む glob が一致しない → NFC 正規化で解決

**検証フロー:**
- ローカルsqlite: dry-run→本番run→再run で冪等性確認
- スポットチェック: セット構成（役割付き）、SiteConfig暗号化（at-rest確認・復号OK）、状態分布
- Cloud SQL: Cloud SQL Auth Proxy 経由で接続、Secret Manager から暗号化キー・DBパスワード取得（本番キーで暗号化）、dry-run一致確認後にインポート実行
- ライブAPI（baiten-gcp.web.app）検証: dashboard 46セット/146実物/20拠点/10顧客、シリアル逆引き連鎖OK、Loyverseトークンがマスク表示（****800e）

**取り込み実績:** マスタ31 / 顧客10 / 拠点20 / 拠点設定14（実クレデンシャル暗号化済）/ セット46 / 実物146 / 構成134。警告3件（重複Part名サフィックス、Untitledセット参照スキップ、SQUARE重複スキップ）— いずれも想定内。

**留意事項:** 本番DBに実決済/POSクレデンシャルが入った（暗号化・APIマスク済）。IAP前段追加とadmin初期パスワード変更の優先度が上がった。

## 💬 使用したプロンプト

**[1]** I prepared data contents of the project database in ../from_notion/extracts/ folder. can you scan and check if you can import?
**[2]** Yap, sounds good. can you start?

## 📁 変更ファイル

- `bomops/bom/management/commands/import_notion.py`（新規: 取り込みコマンド）
- 本番Cloud SQL（baiten-gcp:bomops-pg）へデータ投入: PartMaster/Customer/CustomerSite/SiteConfig/ProductModel/BssSet/PartUnit/BssSetComponent

---
_Generated by /save-task · 2026-06-16T12:28:00+09:00_
