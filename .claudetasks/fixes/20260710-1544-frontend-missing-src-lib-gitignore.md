# 🐛 フロント起動不能を修正: 消えた src/lib/ 復元と .gitignore 根本原因の特定

| 項目 | 内容 |
|------|------|
| **日時** | 2026-07-10 15:44 |
| **カテゴリ** | `fixes` |
| **エンジニア** | Hisato Matsuo |
| **プロジェクト** | `bomops` |

## 📝 作業サマリー

ヘッドレス機での開発サーバ起動（Tailscale IP 経由）に対応し、その後フロントが Vite の import 解決エラーで起動しない問題を修正した。

**環境セットアップ（headless / Tailscale）:**
- Tailscale IP = `100.113.175.45`。backend/frontend とも `0.0.0.0` バインドで公開。
- backend: `venv` 作成 → `requirements.txt` 導入（Django 5.2.16 / Python 3.14）→ sqlite 設定（`config.settings_sqlite`・`SQLITE_PATH=bomops_dev.sqlite3`）で `migrate`（bom 0001〜0009 全 OK）→ superuser `admin` 作成。
- **ポート衝突**: 8000 は別 Django プロジェクト（workcodes/categories API）が使用中 → BOMOps backend を **8001** に、Vite proxy を `BOMOPS_API_TARGET=http://localhost:8001` で追従。
- frontend: `npm install`（334 packages）→ `vite --host 0.0.0.0 --port 5173`。アクセスは **http://100.113.175.45:5173/**（`/api` は Vite が backend にサーバサイド proxy）。

**不具合と根本原因:**
- 症状: `Failed to resolve import "./lib/network-store" from "src/main.tsx"`。
- 調査で **`frontend/web/src/lib/` ディレクトリ全体が作業ツリーから欠落**（git 履歴にも存在せず、未コミット）していると判明。P6「ネットワーク耐性」実装（`20260611-1154`）で新設された `network-store.ts` とフォーム系ヘルパ `form-utils.ts`、および両者のテストが失われていた（依存する他ファイルは生存）。
- **根本原因**: `.gitignore` 17 行目の裸の `lib/`（Python テンプレート由来。本来は build 成果物用）が **`frontend/web/src/lib/` にもマッチ** → フロントのソースディレクトリが常に無視され、コミットされずに失われていた。

**修正内容（消失モジュールを消費側と P6 タスクログから再構築）:**
- `src/lib/network-store.ts`（3層検知ストア）: `navigator.onLine` イベント + API 成功/失敗レポート（連続2回の無応答で offline） + `HEAD /api/v1/health/` の復帰時/5分毎ポーリング。公開 IF は `useSyncExternalStore`/OfflineBanner が要求する `start/stop/subscribe/getStatus/isOffline/reportSuccess/reportFailure` と型 `NetworkStatus`。`client.ts` との循環 import を避け base URL は `VITE_API_BASE_URL` から自前算出。
- `src/lib/form-utils.ts`: `cleanPayload`（文字列トリム＋空値除去。更新は PATCH かつ DRF `blank=True` のため null 化せず**除去**が正）／`dtLocalToIso`・`isoToDtLocal`（datetime-local ⇔ ISO8601 往復）／`applyServerErrors`（`parseApiErrors` の `Record<string,string>` を RHF `setError` へ）。

**検証:** `tsc -b` クリーン（13 → 0 エラー）、`vitest` 12/12 パス、Tailscale IP 経由で `main.tsx` の Vite transform 成功・`network-store.ts`/`form-utils.ts` とも HTTP 200・トップ 200。backend `HEAD /api/v1/health/` → 200。

**残課題 / 推奨フォロー:**
- **`.gitignore` の `lib/` を修正必須**（例: `/lib/` に限定 or `!frontend/web/src/lib/` で除外解除）。しないと再コミットしても再度無視され再発する。
- 再構築した `src/lib/*` は未コミット。P6 で存在したはずの `network-store` / `form-utils` の**ユニットテストも消失**（テスト 23 → 12）。テスト再追加とブランチでのコミットは要判断。
- 別クローン/stash に原本が残っている可能性あり。canonical 化前に diff 比較を推奨。

## 💬 使用したプロンプト

**[1]** please scan this project. work history until now is stored in ./claudetasks. This project is deployed in Google cloud. As this machine is headless, please run development server with tailscale IP based URL.
**[2]** when I access above 100.113.175.45:5173, I hit the following. can you check? [plugin:vite:import-analysis] Failed to resolve import "./lib/network-store" from "src/main.tsx". Does the file exist?

## 📁 変更ファイル

- `frontend/web/src/lib/network-store.ts`（再構築・新規／`.gitignore` により未追跡）
- `frontend/web/src/lib/form-utils.ts`（再構築・新規／`.gitignore` により未追跡）
- `frontend/web/package-lock.json`（`npm install` により更新）
- （生成物・非コミット）`venv/` / `bomops_dev.sqlite3`

---
_Generated by /save-task · 2026-07-10T15:44:00+09:00_
