# 🚀 ダッシュボード運用改修・BOMレシピ導出・本番デプロイ

| 項目 | 内容 |
|------|------|
| **日時** | 2026-07-07 13:32 |
| **カテゴリ** | `tasks` |
| **エンジニア** | Hisato Matsuo |
| **プロジェクト** | `bomops` |

## 📝 作業サマリー

「暫定的だったダッシュボード」を運用ダッシュボードへ改修し、実セット構成から製品BOMレシピを導出して本番投入、バックエンド・フロントエンドとも本番デプロイまで完了した。

**ダッシュボード改修（フロント＋API）:**
- KPIカード・チャートバーをクリック可能にし、絞り込み済み一覧へドリルダウン（セット状態・部品状態・拠点状況・部品カテゴリの全チャート対応）
- 「要対応」パネル新設: 修理中セット／故障部品／準備中拠点を赤カードで表示し該当一覧へリンク（0件時は全て正常表示）
- 「在庫からの組立可能数」テーブル新設: `dashboard/summary/` に `stock_coverage` を追加。必須BOM行ごとの min(在庫数÷必要数量) で組立可能台数を算出し、ボトルネック部品を明示
- 部品実物APIに `category` フィルタ追加（part_master__category 横断）＋一覧画面にカテゴリフィルタUI追加
- 直近イベントのセットコードを詳細ページへリンク化
- OpenAPI スキーマ再生成 → openapi-typescript で型自動生成（手書きなし）

**BOMレシピ導出（`manage.py derive_bom` 新規コマンド）:**
- 実セット46台の搭載構成（観測）と部品マスタの used_in_ai/mini フラグ（意図）を突合して ProductBOM を導出
- 部品コードのファミリ単位（MPC-* 等の互換品番グループ）で出現率評価: ≥50%必須 / ≥20%任意、フラグ付きは必須昇格・未観測でも追加
- 冪等（自然キー upsert）・--dry-run / --replace 対応。必須行が導出できないモデル（BSTAND-UNKNOWN）は正しくスキップ
- 導出結果18行（AI: 必須7+任意3 / Mini: 必須8）。フラグと観測の矛盾2件（MPC: フラグM003 vs 観測M006、BLEG: フラグOLOV vs 観測ニトリ）は[注意]出力で人手確認喚起、承認のうえ観測優先で投入

**本番反映:**
- Cloud SQL Auth Proxy 経由で本番へ derive_bom 実行（dry-run一致確認後）。ProductBOM 18行投入・検証済み
- バックエンド: Cloud Run rev `bomops-api-00002-rl4` デプロイ成功（リネーム後 `bomops_core/` パスでの初デプロイ、ビルド・マイグレーションJob・デプロイ全緑）
- フロントエンド: Firebase Hosting リリース。本番スキーマに StockCoverage 反映確認
- 本番の組立可能数は AI/Mini とも0台 — 必須部品の在庫が全て搭載済みのため（データ検証済みの実態）

**発見・修正した副次バグ:**
- `pytest.ini` の `pythonpath = bomops` がリネーム未反映でテスト実行不能 → `bomops_core` に修正
- `.gitignore` の Python buildout 用 `parts/`（非アンカー）が `frontend/web/src/features/workspace/parts/` の4ソースファイルを誤って除外し git 未追跡だった → `/parts/` にアンカー化して解消

**テスト:** バックエンド36/36（stock_coverage・categoryフィルタ・derive_bom の6テスト新規）、フロント23/23、lint/build 警告ゼロ。

**残課題:** 本セッションの変更は本番デプロイ済みだが git 未コミット（workspace/parts/ の4ファイル含む）。

## 💬 使用したプロンプト

**[1]** it has been a while. can you check the project status and what to do next? I am thinking to improve web app. Current dashboard and other pages are temporal.（→ Dashboard redesign を選択）
**[2]** yes, draft the BOM recipes from the actual set compositions
**[3]** apply it to production as-is
**[4]** yes, deploy both to production

## 📁 変更ファイル

- `bomops_core/bom/views.py`（categoryフィルタ・_stock_coverage・summary拡張）
- `bomops_core/bom/serializers.py`（DashboardStockCoverageSerializer）
- `bomops_core/bom/tests.py`（+6テスト）
- `bomops_core/bom/management/commands/derive_bom.py`（新規）
- `frontend/web/src/features/dashboard/DashboardPage.tsx` / `.module.css`（全面改修）
- `frontend/web/src/features/workspace/parts/PartUnitListPage.tsx`（カテゴリフィルタ）
- `frontend/web/src/api/types.ts` / `schema.d.ts`（型再生成）
- `docs/openapi.yaml`（スキーマ再生成）
- `pytest.ini`（pythonpath修正）/ `.gitignore`（parts/ アンカー化）/ `CLAUDE.md`（derive_bomコマンド追記）
- 本番: Cloud SQL に ProductBOM 18行投入 / Cloud Run `bomops-api-00002-rl4` / Firebase Hosting 新版

---
_Generated by /save-task · 2026-07-07T13:32:11+09:00_
