# 🚀 作成ランチャー（③）と作成後の「次の一手」リンク（②）

| 項目 | 内容 |
|------|------|
| **日時** | 2026-07-08 12:40 |
| **カテゴリ** | `tasks` |
| **エンジニア** | Hisato Matsuo |
| **プロジェクト** | `bomops` |

## 📝 作業サマリー

前回の①（ピッカーからのインライン作成）に続き、UX改善の②③を実装。作成タスクの往復をさらに削減した。

**③ 作成ランチャー（発見性）:**
- ワークスペースホーム上部に「新規作成」バーを追加。5タスク入口（製品モデル/部品/製品セット/顧客/拠点）を1箇所に集約
- 各ボタンは対象ページへ `?new=1` 付きで遷移し、着地と同時に作成ドロワーが自動で開く
- 新規フック `useNewParam`: `?new=1` を1回検知して作成ドロワーを開き、パラメータを消して再表示ループを防止（ref-during-render を避け exhaustive-deps 準拠）
- 4一覧ページ（部品マスタ/製品モデル/製品セット/顧客・拠点）に組込。顧客・拠点はタブに応じて開くドロワーを切替

**② 作成後の「次の一手」リンク（接着剤）:**
- トースト基盤を拡張: `ToastAction`（label/onClick）を success/error にオプション追加。アクション付きは表示8秒・右にピルボタン描画
- 顧客→拠点→セットの連鎖を接続:
  - 顧客作成 → 「この顧客で拠点を追加」→ 拠点フォームを顧客初期選択で開く
  - 拠点作成 → 「この拠点にセットを設置」→ `?new=1&site=<id>` で製品セット作成を設置拠点初期選択で開く（③の仕組み再利用）
- 疎結合設計: ドロワーは連鎖先を知らず、ページが `successAction`/`defaultCustomerId`/`defaultSiteId` を注入。CustomerFormDrawer に successAction、SiteFormDrawer に defaultCustomerId+successAction、BssSetFormDrawer に defaultSiteId を追加

**効果:** 「新規店舗立ち上げ（顧客→拠点→セット）」が画面を探さず一本道に。①（逆方向インライン作成）と合わせ作成の往復がほぼ解消。

**検証:** フロントのみ（バックエンド/DB変更なし）。lint・テスト23/23・ビルド警告ゼロ。開発サーバー稼働中で手動確認可能。

**残課題:** 未コミット・本番未デプロイ。

## 💬 使用したプロンプト

**[1]** はい。２，３、お願いします（前ターンのUX議論 ②次の一手リンク・③作成ランチャー を指す）

## 📁 変更ファイル

- `frontend/web/src/hooks/useNewParam.ts`（新規: ?new=1 検知フック）
- `frontend/web/src/components/toast/{toast-context.ts,ToastProvider.tsx,ToastProvider.module.css}`（ToastAction 対応）
- `frontend/web/src/features/workspace/WorkspaceHome.tsx` / `.module.css`（作成ランチャー）
- `frontend/web/src/features/workspace/parts/PartMasterListPage.tsx`（useNewParam）
- `frontend/web/src/features/workspace/products/ProductModelListPage.tsx`（useNewParam）
- `frontend/web/src/features/workspace/sets/{BssSetListPage,BssSetFormDrawer}.tsx`（useNewParam・?site prefill・defaultSiteId）
- `frontend/web/src/features/workspace/customers/{CustomersPage,CustomerFormDrawer,SiteFormDrawer}.tsx`（?new・successAction・defaultCustomerId）

---
_Generated by /save-task · 2026-07-08T12:40:36+09:00_
