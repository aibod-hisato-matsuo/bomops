# 📄 BOMOpsプロジェクト全体スキャンとER図ギャップ分析

| 項目 | 内容 |
|------|------|
| **日時** | 2026-06-10 15:59 |
| **カテゴリ** | `docs` |
| **エンジニア** | Hisato Matsuo |
| **プロジェクト** | `bomops` |

## 📝 作業サマリー

BOMOps リポジトリの全体スキャンを実施し、`docs/erchart.mmd`（Notion由来ER図・未実装）と現状実装のギャップ分析を行った。

**プロジェクト構成の把握:**
- Django 5 + DRF + PostgreSQL / JWT認証 / drf-spectacular / Docker Compose
- 単一 `bom` アプリに 9 モデル実装済み（PartMaster, PartUnit, ProductModel, ProductBOM, Customer, CustomerSite, BssSet, BssSetComponent, BssSetConfig）
- 全モデルに CRUD ViewSet + フィルタ + admin、カスタムAPI 3本（composition / effective-configs / lookup-by-serial）
- マイグレーション未生成（`migrations/` は `__init__.py` のみ）

**重要な発見 — リポジトリ内に3つのスキーマ定義が併存し不一致:**
1. `docs/erchart.mmd` — Notion生構造（Service_Management にクレデンシャル混在、Equipment_Mgmt、独立DB等）
2. `CLAUDE.md` §3.2 — 正準正規化スキーマ（Organization / Site / SiteConfig / DeviceSet / Unit / MaintenanceEvent / DeployEvent / EquipmentRef）
3. `models.py` — 実装（どちらとも完全一致しない）

**未実装ギャップ:**
- 拠点レベル設定 `SiteConfig`（Loyverse/Square/PayPay等のクレデンシャル、暗号化フィールド）— 現状は per-set の `BssSetConfig`（key-value、暗号化なし）のみ
- 追記型履歴 `MaintenanceEvent` / `DeployEvent` — Dynamic BOM の本質だが完全に未実装
- `EquipmentRef`（機器管理DBとのN:M連携）
- HWスロット割当 `UnitSlotAssignment` — `BssSetComponent.role`（自由テキスト）が部分代替
- その他: PKがint（正準はUUID）、`used_in_ai`/`used_in_mini` フラグ欠落、アプリ分割（structure/configuration/operations/integration）未対応

**推奨次ステップ:** ①正準スキーマの確定（CLAUDE.md §3優先）→ ②Operations層の追加 → ③SiteConfig実装 or 設計判断の明文化 → ④初期マイグレーション生成

## 💬 使用したプロンプト

**[1]** please scan the project for this folder. You can refer docs/erchart.mmd for ER chart which is not yet implemented.

## 📁 変更ファイル

（コード変更なし — 分析・調査のみのセッション）

---
_Generated by /save-task · 2026-06-10T15:59:00+09:00_
