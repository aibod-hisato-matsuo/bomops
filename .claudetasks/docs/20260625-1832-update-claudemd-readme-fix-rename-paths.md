# 📄 CLAUDE.md / README.md 更新と bomops→bomops_core リネーム参照の修正

| 項目 | 内容 |
|------|------|
| **日時** | 2026-06-25 18:32 |
| **カテゴリ** | `docs` |
| **エンジニア** | Hisato Matsuo |
| **プロジェクト** | `bomops` |

## 📝 作業サマリー

「多くの実装を進めてきた」現状に合わせて `CLAUDE.md` と `README.md` を最新化し、ソースツリーのリネーム（`bomops/` → `bomops_core/`）に伴うビルド/デプロイ用パス参照の不整合を修正・検証した。

**ドキュメント更新（CLAUDE.md）:**
- §1 に「プラットフォーム構想（v0.1）」を追加。新規ドキュメント `DOC-PF-001`（Asset Core + Domain Intelligence Gateway + Ops Gateway + Workcode + DIO の5層）を反映し、現在の実装が Asset Core 層＋Ops Gateway の一部であることを明示。空プレースホルダ `domain_gateway/`・`ops_gateway/` と BAITEN Commerce Gateway（`DOC-APP-001`）への導線を記載。
- §2 を実態に更新（Django 5 / drf-spectacular / django-filter / Fernet `EncryptedTextField` / React フロント / 稼働中GCP検証環境）。コマンドを `bomops_core/manage.py` へ修正し、`import_notion`・sqlite テスト設定を追記。実装状況ブロックを新設。
- §5 を仮想ディレクトリ構成から実構成（単一 `bom` アプリ／`bomops_core/`／gatewayプレースホルダ）へ全面置換。
- §7 に `docs/` 内ドキュメント一覧表と `bch-platform` スキルへの導線を追加。

**ドキュメント更新（README.md）:**
- プラットフォーム位置づけ・技術スタック・プロジェクト構造を最新化。全コマンドを `bomops/manage.py` → `bomops_core/manage.py`（PYTHONPATH 含む）に修正。settings 三系統・`import_notion` 節・追記型イベント注記・稼働中GCP URLを追記。

**リネーム参照の修正（ビルド/デプロイが壊れていた箇所）:**
- `Dockerfile` ×3（`PYTHONPATH=/app/bomops_core`・collectstatic・gunicorn `--chdir bomops_core`）
- `docker-compose.yml` ×2（migrate / runserver）
- `scripts/gcp/deploy.sh` ×1（マイグレーションJob `--args`）
- `scripts/gcp/create-superuser.sh` ×1（スーパーユーザーJob `--args`）
- 旧 `bomops/manage.py` 参照が残っていないことを grep で確認。

**検証（Docker ビルド）:**
- `docker build` 成功（依存インストール＋`collectstatic` が `bomops_core/manage.py` 経由で 154ファイルコピー）。
- ランタイム検証 `gunicorn config.wsgi:application --chdir bomops_core --check-config` → exit 0（WSGIアプリが新パスからロード可能）。
- テストイメージは削除。`JSONArgsRecommended` 警告はシェル形式CMD（`$PORT` 展開のため意図的）由来で無害。

**補足:** これらの変更は別途コミット済み（`9dbfaef "finish init imple"`）。

## 💬 使用したプロンプト

**[1]** We have been working many implementations. can you update CLAUDE.md and README.md?
**[2]** yes, fix those path references
**[3]** yes, build it to confirm

## 📁 変更ファイル

- `CLAUDE.md`
- `README.md`
- `Dockerfile`
- `docker-compose.yml`
- `scripts/gcp/deploy.sh`
- `scripts/gcp/create-superuser.sh`

---
_Generated by /save-task · 2026-06-25T18:32:31+09:00_
