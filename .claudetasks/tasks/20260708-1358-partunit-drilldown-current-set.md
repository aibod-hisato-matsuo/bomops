# 🚀 部品実物を製品→部品コード→実物のドリルダウン化＋使用中セット列

| 項目 | 内容 |
|------|------|
| **日時** | 2026-07-08 13:58 |
| **カテゴリ** | `tasks` |
| **エンジニア** | Hisato Matsuo |
| **プロジェクト** | `bomops` |

## 📝 作業サマリー

部品実物ページを、件数増加（146→将来数千件）に耐える段階的ドリルダウンに再構成し、さらに実物にとって最重要の「今どの製品セットに入っているか」を一覧で見えるようにした。

**① ドリルダウン再構成（製品 → 部品コード → 実物一覧）:**
- browse モード: 「製品:」ピル（ファミリ・部品コード数）＋ 部品コード表（部品コード/名/カテゴリ/実物/在庫/故障）。行クリックで units モードへ
- units モード: `?part_master=<id>` で同ページ内フレーム切替。ヘッダー「実物一覧: {code}」＋「← 部品コード一覧へ戻る」、シリアル検索・ステータスフィルタ・実物テーブル。新規作成は部品コードを引き継いで開く
- 部品コード表のカウント基盤: `PartMaster` に `unit_count`/`in_stock_count`/`broken_count` を annotate（作成レスポンス等 未注釈時は実数フォールバック）

**② 使用中セット列（現在地トレーサビリティ）:**
- `PartUnit` に `current_set`（set_id/set_code/role/site_name/customer_name）を追加。`PartUnit → BssSetComponent（unmounted_at が空＝搭載中）→ BssSet` から導出。N+1回避に `set_assignments__bss_set__customer_site__customer` を prefetch
- 実物一覧に「使用中セット」列。セットコード（役割）表示＋クリックで製品セット詳細へ遷移、ホバーで顧客/拠点。未搭載（在庫・取外し済み）は「未搭載」
- 過去の遍歴は既存の「履歴」ボタン（使用履歴タイムライン）が担当。ここは現在地に限定

**検証:** バックエンド **63/63**（実物カウント2件・current_set 2件 新規）、フロント23/23、lint/build クリーン。ライブAPIで「BAITEN STAND→14部品コード」「DIS-M001→実物18件・各実物の使用中セット（9041-0113-0001 pal @ パルシステム小田原 等）」を確認。CLAUDE.md 対応表（Unit 行）更新。

**残課題:** 未コミット・本番未デプロイ（マイグレーションなし・バックエンド+フロント）。

## 💬 使用したプロンプト

**[1]** OK, move on to 部品実物. ... it would be good to filter product -> 部品コード then, a 部品コード is selected, transition to new frame with list. what do you think → please go
**[2]** Thanks. UI is good. In this page, what we want to seek mostly is which 製品セット is this real part used. how can be done this, please have an idea.

## 📁 変更ファイル

- `bomops_core/bom/serializers.py`（PartMaster 実物カウント / PartUnit current_set）
- `bomops_core/bom/views.py`（PartMaster annotate / PartUnit prefetch）
- `bomops_core/bom/tests.py`（実物カウント・current_set の4テスト）
- `frontend/web/src/features/workspace/parts/PartUnitListPage.tsx`（ドリルダウン全面改修・使用中セット列）
- `frontend/web/src/features/workspace/parts/PartUnitFormDrawer.tsx`（defaultPartMasterId）
- `frontend/web/src/api/schema.d.ts` / `docs/openapi.yaml`（再生成）/ `CLAUDE.md`

---
_Generated by /save-task · 2026-07-08T13:58:49+09:00_
